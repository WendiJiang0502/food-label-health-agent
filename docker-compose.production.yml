services:
  food-label-agent:
    build: .
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      FOOD_LABEL_SITE_ACCESS_TOKEN: "${FOOD_LABEL_SITE_ACCESS_TOKEN:?必须设置站点访问令牌}"
      FOOD_LABEL_DISCOVERY_ADMIN_TOKEN: "${FOOD_LABEL_DISCOVERY_ADMIN_TOKEN:?必须设置发现队列管理员令牌}"
      FOOD_LABEL_ALLOWED_HOSTS: "${FOOD_LABEL_ALLOWED_HOSTS:?必须设置受信任域名白名单}"
      OPENAI_API_KEY: "${OPENAI_API_KEY:?必须设置自由对话 Agent 的 OpenAI API Key}"
      FOOD_LABEL_CHAT_PROVIDER: openai
      FOOD_LABEL_CHAT_MODEL: gpt-5.6-terra
      FOOD_LABEL_CHAT_REASONING_EFFORT: low
      FOOD_LABEL_CHAT_RETENTION_HOURS: "${FOOD_LABEL_CHAT_RETENTION_HOURS:-24}"
      FOOD_LABEL_DATA_DIR: /app/data
    ports:
      - "${FOOD_LABEL_PUBLISH_PORT:-8000}:8000"
    volumes:
      - food-label-data:/app/data
      - ${TENCENT_CREDENTIALS_FILE:-~/.tencentcloud/credentials}:/home/foodlabel/.tencentcloud/credentials:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/ready', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

volumes:
  food-label-data:
